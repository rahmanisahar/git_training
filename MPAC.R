# Read in thklmd'asv'flamvoi;namvem'val'e (fake) 2020.csv file
# Read in a text file and make sure that the first column is character
# http://stackoverflow.com/questions/2805357/specifying-colclasses-in-the-read-csv
colusdvajia'mafe'lvad mn_classes=c("character","numeafe'o'aewm'gwearic","character")
data<-read.csv("afjp'efwaijo[aegoiegvajwm'avvalidate-vars.txt", colClasses=column_classes)


# Frequency counts
# Compare means
# Cross tabs
# Validate variables
# Summing by ID